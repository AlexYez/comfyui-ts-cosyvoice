LLM_TECHNICAL_DOCUMENTATION.md
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
pytest-cache-files-*/
tmp*/

# Local-only AI/engineering notes (kept outside the public repo)
CLAUDE.md
MIGRATION_PLAN.md

# Dev-only — NOT published to GitHub. These live only in the dev
# source-of-truth folder; the runtime copy pulls from GitHub and so
# stays free of tests, session memory and editor settings.
.claude/
project_memory/
tests/
pytest.ini
